// ARQUIVO REMOVIDO
// Este arquivo foi substituído pelo novo sistema de roteamento em /routes/AppRouter.tsx
// Mantido apenas para evitar erros de import, mas não deve ser usado

export default function DeprecatedAppRouter() {
  throw new Error('Este AppRouter foi depreciado. Use /routes/AppRouter.tsx');
}